## [23A-R01-001] Worker 07 steering behavior was not promoted into the final candidate

Inline placement is unavailable because the affected product files are not part of PR #1674's retained documentation-only diff. Exact candidate location: `packages/os/scripts/lib/install-state.ts:223` (`DEFAULT_STEERING_FILES`) and `packages/os/scripts/os.ts:497-527` (`getSteering`).

**High / P1 / Architecture**

Candidate `ef2530b136ec2a170915b583abfb2341899bd6ab` still installs and reads `decision.md`, while `getSteering()` emits only environment presence, local markdown, routing text, and the raw core manifest. It does not provide the Worker 07 contract for installed skills, safe node/workspace/channel context, compact update status, or notification preference. The environment section also exposes absolute local paths, contrary to the safe compact context requirement.

This is confirmed as a promotion loss, not an unimplemented idea: PR #1640 (`Installed skills node identity and update summary in steering`) added the dedicated steering runtime context and related tests to `stream/os-distribution`, but merge commit `ef4b0f0352c4eb8bcc5248be7c7962c7fb2968b1` diverges from the final candidate rather than preceding it.

**Concrete risk:** agents bootstrap with stale or incomplete operational context, cannot see installed capability or update state, and may consume unsafe machine-local paths or an obsolete `decision.md` surface. This breaks the authoritative Worker 07 product intent at launch.

**Recommendation:** promote or narrowly reapply the reviewed Worker 07 implementation from PR #1640 onto the next immutable candidate. Remove `decision.md` from fresh-install and steering inputs, emit only redacted portable node and workspace context, and preserve deterministic ordering and compact output.

**Validation:**

- prove a fresh install does not create `decision.md`;
- prove `getSteering()` lists installed skills and safe node/workspace/channel/update context;
- prove absolute home paths, tokens, cookies, and private URLs are absent;
- rerun the Worker 07 focused tests plus installation and steering regressions against the next candidate SHA.

<details>
<summary>Prompt for AI Agents</summary>

Verify `23A-R01-001` against the next exact candidate. If still valid, promote or narrowly reapply PR #1640's Worker 07 behavior without unrelated changes. Remove `decision.md` from fresh install and steering, add safe compact installed-skill/node/workspace/channel/update context, add negative path and secret tests, run the focused install and steering suites, and reply here with the repair PR, exact candidate SHA, commands, and results.

</details>
