## [23A-R01-002] The final candidate dropped the executable distribution-integration lane

Inline placement is unavailable because the affected product files are not part of PR #1674's retained documentation-only diff. Exact candidate location: `packages/os/tests/distribution/lifecycle-contract.test.ts:6-12`.

**High / P1 / Tests**

All seven lifecycle distribution contracts remain `it.todo` in candidate `ef2530b136ec2a170915b583abfb2341899bd6ab`: clean install, update without onboarding, interrupted download, signature or digest rejection, managed-content conflict preservation, channel promotion, and diagnostics redaction.

This is a promotion loss. PR #1663 (`implement distribution integration`) added `scripts/testing/distribution/integration-runner.ts`, added executable distribution-integration coverage, and removed these TODO placeholders on `stream/os-distribution`. Its merge commit `37b083f2eaf0ec406c5de67269fa84a101b05cfa` diverges from the final candidate rather than preceding it.

**Concrete risk:** the final candidate can appear green while the required clean-host and adversarial lifecycle journeys never execute. The observed candidate distribution run passes 77 tests but still reports all seven TODO contracts, so launch evidence does not prove the behaviors Worker 24 was assigned to integrate.

**Recommendation:** promote or narrowly reapply PR #1663's executable integration runner and contracts onto the next immutable candidate, then run the registered platform matrix and preserve the resulting logs and artifacts as candidate-specific evidence.

**Validation:**

- `lifecycle-contract.test.ts` contains no Worker 23a-owned TODO placeholder;
- clean install, update, interrupted download, bad artifact, managed conflict, channel promotion, and redaction journeys execute against real fixture artifacts;
- the registered Linux, macOS, and Windows lanes report exact candidate SHA and artifact identity;
- rerun `bun --cwd packages/os vitest run tests/distribution` and the Worker 24 integration command.

<details>
<summary>Prompt for AI Agents</summary>

Verify `23A-R01-002` against the next exact candidate. If still valid, promote or narrowly reapply PR #1663's distribution integration runner and executable contracts. Do not replace the journeys with source-string assertions. Run the focused distribution suite and registered platform lanes, retain exact-SHA evidence, and reply here with the repair PR, candidate SHA, commands, artifacts, and results.

</details>
