## Worker 23A consolidated repair prompt

Repair only the still-valid Worker 23A findings against the next exact immutable candidate. Preserve lifecycle authority, rollback and retention semantics, managed-content conflict safety, authentication, workspace and user data, and the no-real-machine-mutation boundary unless Ko explicitly runs a checkpoint.

1. **23A-R01-001 — steering promotion loss** (`packages/os/scripts/lib/install-state.ts:223`, `packages/os/scripts/os.ts:497-527`): verify and promote or narrowly reapply PR #1640. Remove `decision.md` from fresh-install and steering inputs. Add deterministic, compact, redacted installed-skills, node, workspace, channel, and update context plus negative path and secret tests.
2. **23A-R01-002 — distribution integration promotion loss** (`packages/os/tests/distribution/lifecycle-contract.test.ts:6-12`): verify and promote or narrowly reapply PR #1663. Replace all Worker 23a-owned TODO placeholders with executable fixture and clean-host journeys and run the registered platform matrix against the exact candidate SHA.
3. **23A-R01-003 — service and link split brain after interrupted activation** (`retention.ts:229-270`, `engine.ts:143-165`, `engine.ts:513-535`): first add a failing crash-boundary regression. Make journal recovery relaunch and health-accept the restored release under the lifecycle lock, and make status surface or reconcile pending recovery.
4. **23A-R01-004 — unbounded lifecycle diagnostics** (`diagnostics.ts:37-52`, `native-lifecycle-endpoint.ts:354-360`): add deterministic bounded rotation and bounded-tail export while preserving redaction, `0600`, JSONL, and symlink containment.

Run focused lifecycle, service, retention, managed-component, steering, and native-endpoint tests; the complete distribution integration suite; typecheck; and the registered Linux, macOS, and Windows candidate lanes. Do not claim success from green bot reviews alone. For each finding, reply in its original PR #1674 thread with `fixed`, `stale`, `needs_verification`, or `waived_by_ko`, naming the repair PR, exact candidate SHA, commands, and evidence. Do not broaden scope or edit the shared finding ledger; Worker 23 owns ledger writes.
