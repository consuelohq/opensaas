*** Begin Patch
*** Update File: packages/os/tests/subagent-orchestration-contract.test.ts
@@
       const result = await executeTool('subagent', input({
         provider: 'grok',
         requestId: 'req_grok_durable',
         instructionPath,
         timeoutMs: 20_000,
       }), options(worktree, env));
 
-      expect(result.ok).toBe(true);
+      expect(result.ok, [result.code, result.data?.status, result.message, result.data?.stderr].filter(Boolean).join(' | ')).toBe(true);
*** End Patch
