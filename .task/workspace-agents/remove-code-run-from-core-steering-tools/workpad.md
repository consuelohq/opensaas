# remove code.run from core steering tools

branch: `task/workspace-agents/remove-code-run-from-core-steering-tools`
stream: `stream/workspace-agents`
pr: https://github.com/consuelohq/opensaas/pull/1298
started: 2026-06-30

## acceptance criteria

- [ ] Workspace core manifest no longer includes `code.run`.
- [ ] OS core manifest no longer includes `code.run`.
- [ ] Full manifests still include `code.run` so the tool remains available outside the core/get-steering surface.
- [ ] `code.call` remains in both core manifests.
- [ ] Generated manifests/docs stay consistent.
- [ ] Focused manifest tests prove the new core exclusion.

## Test-first contract

Behavior under test:
- `buildWorkspaceToolManifest({ write: false }).core.tools` excludes `code.run`.
- `buildToolManifest({ write: false }).core.tools` excludes `code.run`.
- Both full manifests still include `code.run`.
- Both core manifests still include `code.call`.

Existing local pattern to follow:
- `packages/workspace/tests/tool-manifest.test.ts` and `packages/os/tests/tool-manifest.test.ts` maintain `retainedCoreToolNames` and `removedCoreToolNames` contracts.
- Core inclusion is configured by `packages/*/manifests/manifest.config.json` and generated by `packages/*/scripts/generate-tool-manifest.ts`.

New or changed tests:
- Move `code.run` from `retainedCoreToolNames` to `removedCoreToolNames` in both workspace and OS tool manifest tests.
- Add explicit full-manifest retention checks for `code.run` if the existing assertions do not already prove it.

Focused red command:
- `bun --cwd packages/workspace test tests/tool-manifest.test.ts && bun --cwd packages/os test tests/tool-manifest.test.ts`

Expected red failure:
- Current config includes `code.run` in both core manifests, so the focused tests should fail before implementation.

## plan

1. Update tests first and run focused red.
2. Remove `code.run` from both core include lists in manifest configs.
3. Regenerate workspace and OS tool manifests/docs as needed.
4. Run focused green manifest tests and exact JSON verification.
5. Run review and verify, then push/promote.

## current status

- Task started from `stream/workspace-agents`.
- Discovery confirmed `code.run` is included through `packages/workspace/manifests/manifest.config.json` and `packages/os/manifests/manifest.config.json` `core.includeNames`.

## files changed

- TBD

## validation evidence

- TBD

## implementation notes

- Removed code.run from both workspace and OS core include lists.
- Regenerated workspace and OS full/core/workflow manifests and TOOLS docs.
- Corrected stale workspace manifest test expectation from intent to current task.intent source truth so focused manifest validation reflects source truth.

## validation evidence

- Red: `bun --cwd packages/workspace test tests/tool-manifest.test.ts && bun --cwd packages/os test tests/tool-manifest.test.ts` failed after test update because both core manifests still contained code.run; expected 13 core tools but got 14.
- Generation: `bun run --cwd packages/workspace generate-tool-manifest`, `bun run --cwd packages/workspace generate-docs`, `bun run --cwd packages/os generate-tool-manifest`, and `bun run --cwd packages/os generate-docs` passed.
- Green: `bun --cwd packages/workspace test tests/tool-manifest.test.ts` passed: 6 tests.
- Green: `bun --cwd packages/os test tests/tool-manifest.test.ts` passed: 15 tests.
- Exact verification passed: workspace full has code.run, workspace core excludes code.run, OS full has code.run, OS core excludes code.run, and both core manifests retain code.call.

## current diff

- Source config removes code.run from `core.includeNames` in both packages.
- Generated core manifests now have 13 tools each.
- Full manifests still have code.run, with `core: false` after regeneration.
- Tests encode the new core exclusion while preserving full-manifest availability.

## workspace-owned: validation evidence

- TBD
- 2026-06-30 22:20:59 `review.run`: passed — OK
- 2026-06-30 22:21:13 `verify`: passed — OK

## workspace-owned: test selection

- changed files: `.task/tasks/workspace-agents/remove-code-run-from-core-steering-tools.json`, `.task/workspace-agents/remove-code-run-from-core-steering-tools/current.json`, `.task/workspace-agents/remove-code-run-from-core-steering-tools/session.json`, `.task/workspace-agents/remove-code-run-from-core-steering-tools/workpad.md`, `packages/os/manifests/core.manifest.json`, `packages/os/manifests/manifest.config.json`, `packages/os/manifests/tool.manifest.json`, `packages/os/manifests/workflow-bundles.json`, `packages/os/tests/tool-manifest.test.ts`, `packages/workspace/manifests/core-manifest.json`, `packages/workspace/manifests/manifest.config.json`, `packages/workspace/manifests/tool-manifest.json`, `packages/workspace/manifests/workflow-bundles.json`, `packages/workspace/tests/tool-manifest.test.ts`
- matched rules: none
- selected suites: none
- run results: none
- failed suites: none
- zero-suite reason: changed code selected zero suites; add a discoverable test or explicit rule when this is not intentional

## review and verify evidence

- `review.run --base origin/stream/workspace-agents --noTests`: passed with 0 issues and 0 pre-existing issues.
- `verify --base origin/stream/workspace-agents`: passed and wrote publish-valid stamp.
- Verify selected 0 suites automatically, but focused manifest tests were run explicitly and passed as recorded above.

## final pre-publish notes

Files changed:
- `packages/workspace/manifests/manifest.config.json`
- `packages/workspace/manifests/core-manifest.json`
- `packages/workspace/manifests/tool-manifest.json`
- `packages/workspace/manifests/workflow-bundles.json`
- `packages/workspace/tests/tool-manifest.test.ts`
- `packages/os/manifests/manifest.config.json`
- `packages/os/manifests/core.manifest.json`
- `packages/os/manifests/tool.manifest.json`
- `packages/os/manifests/workflow-bundles.json`
- `packages/os/tests/tool-manifest.test.ts`

Result:
- code.run remains in full manifests.
- code.run is absent from Workspace and OS core manifests.
- code.call remains in both core manifests.
