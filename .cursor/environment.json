{
  "install": "yarn install",
  "start": "sudo service docker start && docker compose up -d db redis",
  "terminals": [
    {
      "name": "Dialer Server",
      "command": "yarn start"
    }
  ]
}
