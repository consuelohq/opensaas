name: CI Docs

on:
  push:
    branches:
      - main

  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  changed-files-check:
    uses: ./.github/workflows/changed-files.yaml
    with:
      files: |
        package.json
        packages/consuelo-docs/**
        eslint.config.mjs

  docs-lint:
    needs: changed-files-check
    if: needs.changed-files-check.outputs.any_changed == 'true'
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.11.0
        with:
          access_token: ${{ github.token }}

      - name: Fetch local actions
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install dependencies
        uses: ./.github/actions/yarn-install

      - name: Docs / Lint English MDX files
        run: npx eslint "packages/consuelo-docs/{developers,user-guide,twenty-ui,getting-started,snippets,os}/**/*.mdx" "packages/consuelo-docs/l/*/os/**/*.mdx" --max-warnings 0

      - name: Docs / Check generated OS skill docs
        run: yarn docs:check-os-skill-docs

      - name: Docs / Validate OS navigation routes
        run: yarn docs:validate-os-docs

