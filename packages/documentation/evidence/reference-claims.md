# Reference claim and evidence ledger

Verified: 2026-08-11

The Office OS and security reports were used only as directional maps. The claims below were checked against current source, focused tests, and runtime reproduction.

| Claim | Public page | Source code | Tests | Runtime verification | Directional artifact | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| The installed user-facing lifecycle CLI is `consuelo`; it exposes status, install/update/restart/repair/rollback, channels, notifications, uninstall/dev operations, and bundled-skill add/remove pickers. | `/reference/cli/` | `packages/os/scripts/lifecycle.ts` | `packages/os/tests/lifecycle-help.test.ts`; `packages/os/tests/lifecycle-engine.test.ts`; `packages/os/tests/skill-selection-cli.test.ts` | Current root help plus representative lifecycle and skill-management paths verified on 2026-08-11. | Archived OS Spec and security report (directional only). | preview | Lower-level `scripts/os.ts` remains an agent/operator runtime surface, not the primary user CLI. |
| The canonical home defaults to `~/.consuelo`; configuration uses versioned global, node, and workspace YAML plus machine-local `config.json`. | `/reference/configuration/` | `packages/os/scripts/lib/consuelo-home.ts`; `packages/os/scripts/lib/install-state.ts` | `packages/os/tests/consuelo-home-config.test.ts`; `packages/os/tests/install-state.test.ts` | Generated an isolated home and parsed all three YAML schemas. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| Settings are an overlay, stored as `manifest.overlay.json`, validated against generated manifests, written atomically, and chmod `0600`. | `/reference/configuration/` | `packages/os/scripts/lib/manifest-overlay.ts` | `packages/os/tests/manifest-overlay.test.ts` | Disabled and re-enabled known entries; unknown names failed. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| MCP initializes as protocol `2024-11-05`, lists enabled OS skills, returns empty prompt/resource lists, and only calls OS skills. | `/reference/mcp/` | `packages/os/scripts/lib/mcp-gateway.ts` | `packages/os/tests/mcp-gateway.test.ts` | Exercised initialize, list, call, invalid params, unsupported tool, and unknown method. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| Facade tools are generated from strict definitions with capabilities, timeout, schemas, command mapping, examples, and optional task-session requirements. | `/reference/tools/` | `packages/os/tooling/tool-manifest.schema.json`; `packages/os/scripts/lib/facade/types.ts` | `packages/os/tests/tool-manifest.test.ts`; `packages/os/tests/facade/facade.test.ts` | Parsed the current generated manifest and exercised read, mutation, and task-scoped calls. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| Skills declare permission and approval policy in metadata; `config.json.selectedSkills` is reconciled into `components/installed-skills.json`, steering advertises the active compact catalog, and generated full/core tool manifests retain `consuelo-os-tool-manifest` / `consuelo-os-core-manifest` identities. | `/reference/skills-and-manifests/` | `packages/os/scripts/lib/steering-skills.ts`; `packages/os/scripts/lib/managed-component-install.ts`; `packages/os/manifests/generated/tool.manifest.json` | `packages/os/tests/skills-registry.test.ts`; `packages/os/tests/skill-selection-cli.test.ts`; `packages/os/tests/tool-manifest.test.ts` | Active skill selection/index/steering and generated manifests verified on 2026-08-11. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| Facade `ToolResult` has `apiVersion: 1.0.0`, stable facade codes, `traceId`, timing, stderr, and exit status; skill `CallOutput` has a separate result/error shape. | `/reference/result-and-error-formats/` | `packages/os/scripts/lib/facade/types.ts`; `packages/os/scripts/lib/types.ts` | `packages/os/tests/facade/facade.test.ts`; `packages/os/tests/mcp-gateway.test.ts` | Exercised success, validation, task-session, timeout, command failure, and MCP wrapping. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| `CONSUELO_HOME` is the primary home override, `CONSUELO_OS_HOME` is compatible, and `CONSUELO_OS_PORT` overrides `PORT`. | `/reference/environment-variables/` | `packages/os/scripts/lib/consuelo-home.ts`; `packages/os/scripts/server/env.ts` | `packages/os/tests/consuelo-home-config.test.ts`; `packages/os/tests/local-os-port-cutover.test.ts` | Tested precedence and invalid port bounds. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| The local server binds to `127.0.0.1:46321`; `8960` is legacy; the stable hosted endpoint is `https://os.consuelohq.com/mcp`. | `/reference/urls-and-ports/` | `packages/os/scripts/server/main.ts`; `packages/os/scripts/lib/chatgpt-mcp-connection.ts` | `packages/os/tests/local-os-port-cutover.test.ts`; `packages/os/tests/mcp-gateway.test.ts` | Started the route app on loopback and checked health/MCP behavior. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| Workspace, node, tool, skill, steering, run, trace, artifact, task session, static snapshot, and live OS are distinct product concepts. | `/reference/glossary/` | `packages/os/README.md`; `packages/os/scripts/lib/consuelo-home.ts`; `packages/os/scripts/lib/runtime-state.ts` | `packages/os/tests/install-state.test.ts`; `packages/os/tests/mcp-gateway.test.ts` | Compared terms with installed layout, manifests, execution records, and current user guides. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
