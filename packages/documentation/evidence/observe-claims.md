# Observe claims and evidence

Verified on 2026-07-13. Archived Office and OS design artifacts were used only to find relevant implementation areas; they are not cited as proof of current behavior.

| Claim | Public page | Source code | Tests | Runtime verification | Directional artifact | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| OS skill runs are recorded in `skill_executions` with lifecycle rows in `execution_events`. | Overview; Runs | `packages/os/scripts/lib/runtime-state.ts` | `packages/os/tests/runtime-state.test.ts`; `packages/os/tests/os-call-artifacts.test.ts` | An isolated home recorded a succeeded daily-revenue-brief run and a failed missing-skill run with trace IDs, duration, result, and error fields. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| `doctor:watch`, `doctor:errors`, and `doctor:analytics` expose bounded local run diagnostics. | Overview; Runs; Logs; Debugging failures | `packages/os/scripts/doctor-watch.ts`; `packages/os/scripts/doctor-errors.ts`; `packages/os/scripts/doctor-analytics.ts` | `packages/os/tests/doctor-logs.test.ts`; `packages/os/tests/doctor-redaction.test.ts` | The seeded runs appeared in all three commands, including artifact count, `SKILL_NOT_FOUND`, and aggregated duration/error output. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Workspace facade calls use a separate `tool_traces` store with task, branch, worktree, result, stderr, duration, and optional token fields. | Overview; Tool calls | `packages/workspace/scripts/context.js`; `packages/workspace/scripts/trace-home/db.ts` | `packages/workspace/tests/context-trace.test.js`; `packages/workspace/tests/trace-home.test.ts`; `packages/workspace/tests/trace-watch.test.ts` | A fixture error row was queried by trace ID with raw input, resolved input, result, stderr, task session, branch, worktree, and token counts. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Trace IDs correlate related records but do not collapse OS runs and workspace tool calls into one universal database. | Overview; Traces; Tool calls | `packages/os/scripts/lib/runtime-state.ts`; `packages/workspace/scripts/context.js` | `packages/os/tests/runtime-state.test.ts`; `packages/workspace/tests/context-trace.test.js` | Both stores were exercised independently with explicit trace IDs. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Hosted Trace Sites browser reads go through the authenticated Consuelo gateway at `/gateway/traces/*`, not direct local runtime URLs. | Traces; Debugging failures | `packages/os/scripts/server/routes/traces.ts`; `packages/os/scripts/lib/trace-sites-browser-client.ts` | `packages/os/tests/trace-sites-runtime-boundary.test.ts`; `packages/os/tests/trace-sites-browser-client.test.ts`; `packages/os/tests/trace-sites-gateway-live-endpoints.test.ts` | Route and browser client contracts were inspected and focused tests cover the gateway-only boundary. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Trace Sites can report live, stale, degraded, bridge-required, cached aggregate, or unavailable states. | Traces; Debugging failures | `packages/os/scripts/lib/trace-sites-browser-client.ts`; `packages/os/scripts/lib/trace-sites-gateway-read-layer.ts` | `packages/os/tests/trace-sites-browser-client.test.ts`; `packages/os/tests/trace-sites-gateway-read-layer.test.ts` | Focused contracts exercised bridge-required and `TRACE_STORE_UNAVAILABLE`, plus cached aggregate and live fallback behavior. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Artifact versions are immutable records with trace provenance, parent/current relationships, hash, byte size, and storage paths. | Artifacts; Debugging failures | `packages/os/scripts/lib/artifacts.ts`; `packages/os/scripts/artifacts.ts` | `packages/os/tests/artifacts.test.ts`; `packages/os/tests/os-call-artifacts.test.ts`; `packages/os/tests/artifact-render.test.ts` | A generated artifact returned one current immutable version with `traceId`, `contentSha256`, byte size, and local storage path. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Structured OS logs and doctor output pass through current redaction rules before serialization. | Logs; Debugging failures | `packages/os/scripts/lib/redaction.ts`; `packages/os/scripts/server/logger.ts`; `packages/os/scripts/lib/facade/logger.ts` | `packages/os/tests/redaction.test.ts`; `packages/os/tests/doctor-redaction.test.ts`; `packages/os/tests/doctor-logs.test.ts` | Isolated doctor JSON output redacted a sensitive path as `[REDACTED_SECRET]`. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| A caller timeout is not proof that the underlying operation failed; recorded state must be checked before retrying. | Debugging failures | `packages/os/scripts/lib/runtime-state.ts`; `packages/workspace/scripts/context.js` | `packages/os/tests/runtime-state.test.ts`; `packages/workspace/tests/context-trace.test.js` | Runtime records were inspected independently from the original caller response. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
