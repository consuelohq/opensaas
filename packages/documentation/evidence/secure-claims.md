# Secure claim and evidence ledger

Verified on 2026-07-13. The archived OS Spec and Network and Security Report were used only to locate implementation areas. Current source and focused tests decide the public claim.

| Claim | Public page | Source code | Tests | Runtime verification | Directional artifact | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Identity, authentication, authorization, approval, and transport are separate boundaries. | `/secure/` | `packages/os/scripts/lib/security-gateway.ts`; `packages/os/cloudflare/os-device-authority/src/services/mcp-proxy.ts` | `security-gateway.test.ts`; `os-device-authority-worker.test.ts` | Focused route, credential, OAuth, and policy contracts passed. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
| Static Sites snapshots are edge objects; live OS routes use authenticated or signed connector paths. | `/secure/security-model/` | `packages/os/scripts/lib/workspace-cloudflare-edge-router.ts` | `cloudflare-edge-router.test.ts`; `workspace-edge-sites-gateway-integration.test.ts` | Static and live route paths exercised independently. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
| Tool access is workspace/caller/app scoped and unknown tool scopes fail closed. | `/secure/access-and-permissions/` | `packages/os/scripts/lib/security-gateway.ts`; `packages/os/scripts/lib/mcp-gateway.ts` | `security-gateway.test.ts`; `mcp-gateway.test.ts` | Manifest-backed scope resolution and denial exercised. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
| Credentials can be active, rotated, or revoked, and raw credential material is not exposed by status records. | `/secure/credentials/` | `packages/os/scripts/lib/security-gateway.ts` | `security-gateway.test.ts`; `mcp-gateway.test.ts` | Issue, status, rotation, revocation, expiry, and audit-failure paths exercised. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
| The legacy generic `MCP_BEARER_TOKEN` is not accepted as a fallback. | `/secure/credentials/` | `packages/os/scripts/lib/security-gateway.ts` | `security-gateway.test.ts` | Explicit negative contract passed. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
| Device approval requires a local device key and stronger authentication; username/password approval is rejected. | `/secure/approvals/` | `packages/os/cloudflare/os-device-authority/src/security/device-auth.ts` | `os-device-approval-auth-hardening-contract.test.ts` | Forged, anonymous, mismatched-key, and password paths rejected. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
| The local service uses loopback and a managed workspace can use an outbound connector. | `/secure/nodes-and-network-access/` | `packages/os/scripts/lib/workspace-connector-transport.ts`; `packages/os/scripts/lib/security-gateway.ts` | `workspace-gateway-contract.test.ts`; `install-workspace-bootstrap-contract.test.ts` | Loopback origin, Caddy target, connector planning, and secret placement exercised. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
| Tailscale is a private Tailnet path, not the default public MCP ingress. | `/secure/tailscale/` | `packages/os/TOOLS.md`; `packages/os/skills/office/references/agents.md` | `packages/os/tests/tool-manifest.test.ts`; `packages/os/tests/office-skill.test.ts` | Private Tailscale Serve and durable Office publish contracts inspected and exercised. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
| Hosted MCP uses OAuth authorization code + PKCE, workspace membership, scoped bearer access, and a signed connector hop. | `/secure/hosted-mcp-ingress/` | `packages/os/cloudflare/os-device-authority/src/services/mcp-oauth.ts`; `packages/os/cloudflare/os-device-authority/src/services/mcp-proxy.ts` | `os-device-authority-worker.test.ts`; `mcp-gateway.test.ts` | OAuth discovery, exchange, resource checks, proxy signing, and scope denial exercised. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
| Provider IP policy is defense in depth, not user authentication or tool authorization. | `/secure/hosted-mcp-ingress/` | `packages/os/scripts/lib/workspace-cloudflare-provisioning.ts`; `packages/os/cloudflare/os-device-authority/src/services/mcp-proxy.ts` | `cloudflare-provisioning-contract.test.ts`; `os-device-authority-worker.test.ts` | Policy generation and OAuth/scope contracts verified separately; no production provider dashboard claim is made. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
| Signed requests reject changed bodies, stale timestamps, replayed nonces, and identity mismatches. | `/secure/security-reference/` | `packages/os/scripts/lib/security-gateway.ts` | `security-gateway.test.ts` | Positive and negative signature contracts passed. | Archived Network and Security Report and OS Spec (directional only). | preview | Current implementation is authoritative. |
