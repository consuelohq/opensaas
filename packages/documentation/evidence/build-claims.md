# Build with OS claim and evidence ledger

Verified on 2026-08-11. Archived product reports guided discovery but are not evidence in this ledger.

Areas covered: Tools, Skills, Steering, Workflows, Shared memory and context, Files and artifacts, and Approvals.

| Claim | Public page | Source code | Tests | Runtime verification | Directional artifact | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Agents bootstrap with `get_steering`, discover non-core tools with `tools.search`, and invoke typed operations through `call`. | Overview; How tools work | `packages/os/README.md`; `packages/os/scripts/os.ts`; `packages/os/tooling/dev-tool-manifest.json` | `packages/os/tests/os-get-steering-trace.test.ts`; `packages/os/tests/tools-search-v2.test.ts`; `packages/os/tests/tool-manifest.test.ts` | Current steering assembly, core manifest, and facade schemas inspected. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Task-scoped tools use a top-level `taskSession` to stay inside the isolated task worktree. | Workspace; Workflows | `packages/os/tooling/dev-tool-manifest.json`; `packages/os/manifests/workflow-bundles.json` | `packages/os/tests/tool-manifest.test.ts`; `packages/os/tests/workflow-intent.test.ts` | This documentation task used the same scoped facade contract. | Archived OS Spec (directional only). | shipped | Current implementation is authoritative. |
| Browser work uses typed operations and accessibility references for known URLs and authenticated UI verification. | Browser | `packages/os/skills/browser/SKILL.md`; `packages/os/tooling/dev-tool-manifest.json` | `packages/os/tests/tool-manifest.test.ts`; `packages/os/tests/trace-sites-browser-client.test.ts` | Current browser tool definitions and operating guide inspected. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Artifacts is the current source-first durable-output workflow and typed tool surface; the older Office naming is no longer present in the generated tool manifest. | Artifacts | `packages/os/skills/artifacts/SKILL.md`; `packages/os/manifests/generated/tool.manifest.json` | `packages/os/tests/artifacts-skill.test.ts`; `packages/os/tests/tool-manifest.test.ts` | Current Artifacts skill and `artifacts.*` facade entries verified on 2026-08-11. | Archived OS Spec (directional only). | preview | Office links are redirected to Artifacts. |
| Media tools declare runtime requirements and return structured media or artifact outputs. | Media | `packages/os/tooling/media-tool-manifest.json` | `packages/os/tests/media/17-media-ingest-source-capture.test.ts`; `packages/os/tests/media/23-vision-pose-mediapipe.test.ts` | Current manifest and focused media tests inspected. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| A skill separates `SKILL.md` guidance from `skill.json` metadata and optional script implementation. | Skills pages | `packages/os/scripts/lib/skills.ts`; `packages/os/skills/skill-creator/SKILL.md` | `packages/os/tests/skills-registry.test.ts`; `packages/os/tests/skill-migration.test.ts` | Registry generation and current bundled metadata inspected. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Skill Templates can be added or removed after install with `consuelo add skill` and `consuelo remove skill`; `config.json.selectedSkills` is reconciled into the active installed index and visible `~/Consuelo/Skills` tree, while locally modified managed copies are preserved for review. | Install a skill; Skill Templates | `packages/os/scripts/lib/skill-selection.ts`; `packages/os/scripts/lib/managed-component-install.ts`; `packages/os/scripts/lib/steering-skills.ts` | `packages/os/tests/skill-selection-cli.test.ts`; `packages/os/tests/skills-registry.test.ts` | Direct and picker-based add/remove flows, active index, visible materialization, and steering discovery verified on 2026-08-11. | Archived OS Spec (directional only). | shipped | Current implementation is authoritative. |
| Workspace steering loads `system_prompt.md`, then `decision.md`, then other Markdown files alphabetically; legacy `steering.md` is ignored. | Steering pages | `packages/os/scripts/os.ts` | `packages/os/tests/os-get-steering-trace.test.ts` | Current loader and repeated-call guard inspected. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Arbitrary repository instruction files are not automatically merged into OS `get_steering`. | Project steering | `packages/os/scripts/os.ts`; `AGENTS.md` | `packages/os/tests/os-get-steering-trace.test.ts` | OS loader scope compared with current repository guidance. | Archived OS Spec (directional only). | shipped | Current implementation is authoritative. |
| The current workflow registry exposes the task workflow, mapping task intent to ordered roles, tools, and lifecycle subscriptions. | Workflows | `packages/workspace/tooling/workflows.json` | `packages/os/tests/workflow-intent.test.ts`; `packages/os/tests/task-manifest-workflow-roles.test.ts` | Current task workflow registry inspected on 2026-08-11. | Archived OS Spec (directional only). | preview | Skills may orchestrate procedures without being registered workflow bundles. |
| Saved context is intentional searchable material; local trace context can be filtered separately. | Shared memory and context | `packages/os/tooling/dev-tool-manifest.json`; `packages/workspace/scripts/context.js` | `packages/workspace/tests/context-trace.test.js` | Typed context schema and trace filters inspected. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Local artifacts are versioned outputs linked to a trace and skill; app-visible publication is optional and fails without deleting the local artifact. | Files and artifacts | `packages/os/scripts/lib/artifacts.ts`; `packages/os/scripts/lib/cloud-artifacts.ts` | `packages/os/tests/artifacts.test.ts`; `packages/os/tests/cloud-artifacts.test.ts` | Local and app-file artifact paths inspected and exercised by focused tests. | Archived OS Spec (directional only). | preview | Current implementation is authoritative. |
| Approval-gated runtime skills return `APPROVAL_REQUIRED` before execution; the current result does not automatically resume later. | Approvals | `packages/os/scripts/os.ts`; `packages/os/scripts/lib/local-guardrails.ts` | `packages/os/tests/local-guardrails.test.ts`; `packages/os/tests/tool-manifest.test.ts` | Current stop-before-execution code path inspected. | Archived OS Spec (directional only). | shipped | Current implementation is authoritative. |
