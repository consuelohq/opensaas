# Connect claim and evidence ledger

Verified on 2026-07-13. Archived OS and security reports guided discovery but are not evidence in this ledger.

| Claim | Public page | Source code | Tests | Runtime verification | Directional artifact | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Codex, Cursor, Claude Code, OpenCode, Factory Droid, and Gemini CLI have native local adapters using `consuelo-os-mcp`; Pi is detected but unsupported. | Agent pages | `packages/os/scripts/lib/local-agent-connectivity.ts` | `packages/os/tests/local-agent-connectivity.test.ts`; `packages/os/tests/install-state.test.ts`; `packages/os/tests/installer-local-agent-connectivity.test.ts` | OpenCode isolated installer and MCP handshake; all adapter shapes covered by focused tests. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| `verified` requires MCP initialize, `tools/list`, and at least one tool. | Local agent pages | `packages/os/scripts/lib/local-agent-connectivity.ts` | `packages/os/tests/local-agent-connectivity.test.ts` | Probe behavior exercised with real spawned MCP fixture. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| ChatGPT connects through HTTPS MCP and OAuth, with the central endpoint resolving an approved account workspace to a signed connector route. | `/connect/agents/chatgpt/` | `packages/os/cloudflare/os-device-authority/src/services/mcp-oauth.ts`; `packages/os/cloudflare/os-device-authority/src/app.ts` | `packages/os/tests/os-device-authority-worker.test.ts`; `packages/os/tests/mcp-gateway.test.ts` | OAuth callback, token exchange, routing, and proxied MCP request exercised in worker tests. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| ChatGPT is the only non-placeholder cloud connector in current Settings. | Connect overview; Other agents | `packages/os/scripts/lib/settings-snapshot.ts` | `packages/os/tests/settings-site.test.ts` | Snapshot generated from isolated Consuelo home fixture. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| GitHub is available through `workspace.github` when runtime GitHub authentication exists. | `/connect/connectors/github/` | `packages/os/tooling/dev-tool-manifest.json`; `packages/os/scripts/lib/github.js` | `packages/os/tests/tools-search-v2.test.ts`; workspace task/review suites | Current workspace task used the typed GitHub facade during delivery. | Archived OS Spec and security report (directional only). | shipped | Current implementation is authoritative. |
| Slack currently supports outbound webhook posting only. | `/connect/connectors/slack/` | `packages/os/tools/slack.py` | No focused automated test; adapter source inspection only. | Adapter source inspected; no live message sent to avoid an external side effect. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| Google Drive, Gmail, and Calendar service connectors are not self-service; Google workspace approval requests identity scopes only. | Connector pages | `packages/os/docs/env-capability-matrix.md`; `packages/os/cloudflare/os-device-authority/src/constants.ts` | `packages/os/tests/os-device-authority-worker.test.ts` | OAuth scope assertion inspected and exercised in focused tests. | Archived OS Spec and security report (directional only). | unresolved | Current implementation is authoritative. |
| There is no generic self-service service connector registry. | Connectors overview; Additional connectors | `packages/os/integrations.md`; `packages/os/scripts/lib/capabilities.ts` | No focused automated test; capability source and manifests inspected. | Current capability health and manifest surfaces inspected. | Archived OS Spec and security report (directional only). | shipped | Current implementation is authoritative. |
| The first managed home node is `home`; later nodes are `member`; enrollment reports `created` or `reconnected`. | Node pages | `packages/os/scripts/lib/install-state.ts`; `packages/os/cloudflare/os-device-authority/src/app.ts` | `packages/os/tests/os-device-authority-worker.test.ts`; `packages/os/tests/install-state.test.ts` | Multi-node worker flow exercised by focused tests. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| Managed local node transport is recorded as `cloudflare-tunnel` or `websocket-relay`. | `/connect/nodes/local-nodes/` | `packages/os/scripts/install.ts`; `packages/os/scripts/lib/install-state.ts` | `packages/os/tests/install-workspace-bootstrap-contract.test.ts`; `packages/os/tests/cloudflare-connector-transport-contract.test.ts` | Bootstrap transport selection exercised by focused tests. | Archived OS Spec and security report (directional only). | preview | Current implementation is authoritative. |
| Cloud mode is assisted and exits to contact before local provisioning. | `/connect/nodes/cloud-nodes/` | `packages/os/scripts/install.ts` | `packages/os/tests/installer-onboarding-ui.test.ts`; `packages/os/scripts/onboarding-flow.test.ts` | Installer branch covered by focused UI/flow tests. | Archived OS Spec and security report (directional only). | unresolved | Current implementation is authoritative. |
