---
title: Workflows
description: Use manifest-backed workflow bundles for repeated, ordered repository work.
status: preview
verifiedAt: 2026-08-15
evidence:
  - source: packages/workspace/tooling/workflows.json
    tests:
      - packages/os/tests/workflow-intent.test.ts
      - packages/os/tests/task-manifest-workflow-roles.test.ts
    runtime: The current workflow registry, canonical session constructor, task compatibility alias, and task hook roles were verified on 2026-08-15.
---

A **workflow bundle** maps a repeated kind of work to ordered roles, typed tools, and lifecycle events. It is different from a skill: a skill teaches judgment and procedure, while a workflow bundle registers a concrete lifecycle/tool sequence.

## Current workflow bundle

The current registry exposes the **task** workflow, with aliases such as `repo` and `code`.

```text
stream context
  → session start (`kind: task`)
  → scoped workpad and test contract
  → research
  → red test or documented waiver
  → implementation
  → focused verification
  → review and full verify
  → push
  → stream PR
  → finish
```

`session.start({ kind: "task" })` is the canonical task constructor and returns a `taskSession` tied to an isolated branch and worktree. `task.start` remains a compatibility alias for existing callers. Pass the returned session to task-scoped tools. Use `session.start({ kind: "work", path })` for ordinary node-local filesystem work outside managed repositories; it creates metadata and affinity only, not a branch, worktree, or PR. Workflow hooks add lifecycle guidance; they do not replace validation or review.

## Workflow versus skill

Skills such as [Branch](/build/skills/bundled/branch/), [Artifacts](/build/skills/bundled/artifacts/), and [Teach](/build/skills/bundled/teach/) can orchestrate many tools without each being a registered workflow bundle.

Use `packages/workspace/tooling/workflows.json` as the source of truth for registered bundles. Do not infer a workflow merely because a skill describes a sequence.
