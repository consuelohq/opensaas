---
title: Salesforce
description: Use Salesforce CLI and APIs today while native Salesforce tools are planned.
status: preview
verifiedAt: 2026-07-14
evidence:
  - source: packages/os/manifests/generated/tool.manifest.json
    tests:
      - packages/os/tests/tool-manifest.test.ts
    runtime: 'The current OS tool manifest was inspected on 2026-07-14; no native `salesforce` provider family is currently published.'
---

> **Native Consuelo tool: Planned**  
> **Available today: Yes — through the official Salesforce CLI and platform APIs.**

Use Salesforce when you need to authenticate an org, query records, inspect metadata, deploy source, and automate carefully scoped CRM work.

Consuelo plans to provide a focused `salesforce` tool family. You do not need to wait for that native wrapper: an agent can use the official interface locally now, then turn the smallest proven workflow into a typed OS tool.

## Ask your agent

```text
Set up Salesforce for this workspace using the official interface linked below.
Keep authentication on the local node and never print the credential.
Start with one read-only verification command.
Then create a typed Consuelo tool exposing only the operations I approve.
Separate read methods from write methods, add structured errors and focused tests,
and require approval for external, destructive, billing, permission, or deployment changes.
```

## Install and authenticate

Install Salesforce CLI, run `sf org login web` for an interactive org, and give the org a clear alias. For automation, use an organization-approved connected app rather than sharing a personal session.

Store service credentials with [Apple Keychain and API keys](/secure/apple-keychain-and-api-keys/) or another local process-owned mechanism. Do not put a token in steering, a skill file, a conversation, or a command argument that may be logged.

## Verify before building a tool

Run `sf org list`, then a bounded query such as `sf data query --query "SELECT Id, Name FROM Account LIMIT 5" --target-org <alias>`.

A successful login is not enough. Confirm the selected account, team, project, region, or workspace before the tool can act.

## Proposed Consuelo methods

```text
salesforce.orgs.list
salesforce.records.query
salesforce.records.get
salesforce.metadata.describe
salesforce.source.deploy
```

Useful typed inputs:

```text
org
object
query
fields
limit
wait
dryRun
approval
```

The native wrapper should return structured results, redact secret material, expose exact provider errors, and make account or project selection explicit.

## Writes and approvals

Record mutations, bulk jobs, metadata deploys, permission changes, and destructive changes require approval. Default queries to explicit fields and limits.

## Native Consuelo roadmap

The planned tool will wrap proven provider operations rather than hide the underlying contract. Until it ships, the official CLI or API remains the source of truth for authentication, flags, limits, and provider behavior.

## Official documentation

[Salesforce official documentation](https://developer.salesforce.com/tools/salesforcecli)
