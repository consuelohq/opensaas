---
title: Notion
description: Use the Notion API today while native Notion tools are planned.
status: preview
verifiedAt: 2026-07-14
evidence:
  - source: packages/os/tooling/dev-tool-manifest.json
    tests:
      - packages/os/tests/tool-manifest.test.ts
    runtime: 'The current OS tool manifest was inspected on 2026-07-14; no native `notion` provider family is currently published.'
---

> **Native Consuelo tool: Planned**  
> **Available today: Yes — through the official Notion API and connection model.**

Use Notion when you need to search and read pages, query data sources, create or update content, and automate a deliberately shared part of a Notion workspace.

Consuelo plans to provide a focused `notion` tool family. You do not need to wait for that native wrapper: an agent can use the official interface locally now, then turn the smallest proven workflow into a typed OS tool.

## Ask your agent

```text
Set up Notion for this workspace using the official interface linked below.
Keep authentication on the local node and never print the credential.
Start with one read-only verification command.
Then create a typed Consuelo tool exposing only the operations I approve.
Separate read methods from write methods, add structured errors and focused tests,
and require approval for external, destructive, billing, permission, or deployment changes.
```

## Install and authenticate

Create an internal connection for one workspace or an OAuth connection for a distributable integration. Share only the pages or data sources the connection needs.

Store service credentials with [Apple Keychain and API keys](/secure/apple-keychain-and-api-keys/) or another local process-owned mechanism. Do not put a token in steering, a skill file, a conversation, or a command argument that may be logged.

## Verify before building a tool

Read the current bot user or one explicitly shared page before enabling search, create, update, comment, or file operations.

A successful login is not enough. Confirm the selected account, team, project, region, or workspace before the tool can act.

## Proposed Consuelo methods

```text
notion.search
notion.pages.get
notion.dataSources.query
notion.pages.create
notion.pages.update
```

Useful typed inputs:

```text
pageId
dataSourceId
query
properties
pageSize
cursor
dryRun
approval
```

The native wrapper should return structured results, redact secret material, expose exact provider errors, and make account or project selection explicit.

## Writes and approvals

Creating or updating pages, comments, databases, and permissions requires approval. A token alone does not grant access to content that was not shared with the connection.

## Native Consuelo roadmap

The planned tool will wrap proven provider operations rather than hide the underlying contract. Until it ships, the official CLI or API remains the source of truth for authentication, flags, limits, and provider behavior.

## Official documentation

[Notion official documentation](https://developers.notion.com/guides/get-started/overview)
