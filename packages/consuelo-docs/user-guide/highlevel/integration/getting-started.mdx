---
title: Getting Started
description: Connect your HighLevel location to Consuelo and run your first import.
---

Use this guide to connect a HighLevel location to Consuelo for the first time.

## Before you connect

Make sure you have:

- access to the Consuelo workspace where the integration should live
- a HighLevel user who can authorize access for the target location
- a plan for your first import so you know whether to bring everything in or start with a filtered subset

## Step 1: open the HighLevel integration settings

In Consuelo, open the HighLevel integration settings page and click **Connect HighLevel**.

## Step 2: approve the OAuth flow

Consuelo starts the HighLevel OAuth flow and sends you to the location chooser. Approve the connection for the correct location, then return to Consuelo.

## Step 3: confirm the connection state

After the callback completes, the integration should show that the location is connected.

A healthy connection gives you:

- a connected state instead of disconnected
- the location identifier returned by HighLevel
- a connected timestamp

## Step 4: run your first contact import

Start with a first import so Consuelo can build the sync map between HighLevel contact ids and your People records.

If you want to limit the first run, use a tag filter before you import.

## Step 5: review the sync log

After the import completes, review the sync log to confirm:

- how many records were imported
- how many were updated instead of created
- how many were skipped
- whether any errors need cleanup before the next run

## What permissions the integration asks for

The OAuth flow requests the permissions required for contacts, conversations, messages, and location access inside HighLevel.

## Next steps

- [Import Contacts from HighLevel](/user-guide/highlevel/integration/how-tos/import-contacts-from-highlevel)
- [Field Mapping](/user-guide/highlevel/integration/capabilities/field-mapping)
- [Set Up Pipeline Sync](/user-guide/highlevel/integration/how-tos/set-up-pipeline-sync)




