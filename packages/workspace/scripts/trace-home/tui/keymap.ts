import type { KeyEvent } from '@opentui/core';
export type KeyIntent = 'quit' | 'pause' | 'up' | 'down' | 'refresh' | 'failed' | 'group' | 'help' | 'escape' | 'search' | 'branch' | 'tool' | 'copy' | 'open' | 'raw-json' | 'none';
export function keyToIntent(key: Pick<KeyEvent, 'name' | 'ctrl' | 'sequence'>): KeyIntent { if (key.ctrl && key.name === 'c') return 'quit'; if (key.name === 'q') return 'quit'; if (key.name === 'space') return 'pause'; if (key.name === 'up' || key.name === 'k') return 'up'; if (key.name === 'down' || key.name === 'j') return 'down'; if (key.name === 'r') return 'refresh'; if (key.name === 'f') return 'failed'; if (key.name === 'g') return 'group'; if (key.name === '?' || key.sequence === '?') return 'help'; if (key.name === 'escape') return 'escape'; if (key.name === '/' || key.sequence === '/') return 'search'; if (key.name === 'b') return 'branch'; if (key.name === 't') return 'tool'; if (key.name === 'c') return 'copy'; if (key.name === 'return') return 'open'; if (key.name === 'y') return 'raw-json'; return 'none'; }
