import type { GroupMode, TraceHomeBuildOptions, TraceHomeModel } from '../types';
export type TraceHomeState = { paused: boolean; selectedIndex: number; search: string; failedOnly: boolean; branchFilter?: string; toolFilter?: string; group: GroupMode; help: boolean; rawJson: boolean };
export type TraceHomeAction = { type: 'pause' } | { type: 'move'; delta: number; rowCount: number } | { type: 'failed' } | { type: 'group' } | { type: 'help' } | { type: 'close' } | { type: 'search'; value: string } | { type: 'branch'; value?: string } | { type: 'tool'; value?: string } | { type: 'raw-json' };
export const initialTraceHomeState: TraceHomeState = { paused: false, selectedIndex: 0, search: '', failedOnly: false, group: 'none', help: false, rawJson: false };
export function reduceTraceHomeState(state: TraceHomeState, action: TraceHomeAction): TraceHomeState { switch (action.type) { case 'pause': return { ...state, paused: !state.paused }; case 'move': return { ...state, selectedIndex: Math.min(Math.max(state.selectedIndex + action.delta, 0), Math.max(action.rowCount - 1, 0)) }; case 'failed': return { ...state, failedOnly: !state.failedOnly, selectedIndex: 0 }; case 'group': return { ...state, group: state.group === 'none' ? 'branch' : state.group === 'branch' ? 'tool' : 'none' }; case 'help': return { ...state, help: !state.help }; case 'close': return { ...state, help: false }; case 'search': return { ...state, search: action.value, selectedIndex: 0 }; case 'branch': return { ...state, branchFilter: action.value || undefined, selectedIndex: 0 }; case 'tool': return { ...state, toolFilter: action.value || undefined, selectedIndex: 0 }; case 'raw-json': return { ...state, rawJson: !state.rawJson }; default: return state; } }
export function stateToBuildOptions(state: TraceHomeState, model?: TraceHomeModel): TraceHomeBuildOptions { return { live: !state.paused, selectedIndex: Math.min(state.selectedIndex, Math.max((model?.visibleRows.length || 1) - 1, 0)), search: state.search, failedOnly: state.failedOnly, branchFilter: state.branchFilter, toolFilter: state.toolFilter, group: state.group, rawJson: state.rawJson }; }
