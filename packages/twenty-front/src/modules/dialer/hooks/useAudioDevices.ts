// TODO: DEV-712 stub — implemented in 2.10
export const useAudioDevices = () => ({
  devices: [] as MediaDeviceInfo[],
  selectedMic: null as string | null,
  selectedSpeaker: null as string | null,
});
