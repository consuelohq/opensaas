---
import { launchFaq } from '../../data/launch-content';
import type { LaunchFaqItem } from '../../data/launch-content';

type Props = {
  sectionId?: string;
  title?: string;
  intro?: string;
  items: LaunchFaqItem[];
};

const {
  sectionId,
  title = launchFaq.title,
  intro = launchFaq.intro,
  items,
} = Astro.props as Props;
---

<section class="launch-section" id={sectionId} data-motion="section-reveal">
  <div class="launch-content launch-content--wide launch-faq">
    <div class="launch-faq__intro">
      <p class="launch-eyebrow" data-motion="split-line">{launchFaq.eyebrow}</p>
      <h2 data-motion="split-line">{title}</h2>
      {intro && <p class="launch-copy" data-motion="split-line">{intro}</p>}
    </div>

    <div class="launch-faq__list">
      {items.map((item) => (
        <details class="launch-faq__item" data-motion="faq">
          <summary data-motion="faq-trigger">
            <span class="launch-faq__icon" aria-hidden="true" data-motion="faq-icon">
              <span></span>
              <span></span>
            </span>
            <span data-motion="faq-question split-line">{item.question}</span>
          </summary>
          <p class="launch-copy launch-faq__body" data-motion="faq-body">{item.answer}</p>
        </details>
      ))}
    </div>
  </div>
</section>

<style>
  .launch-faq {
    display: grid;
    gap: 1.35rem;
  }

  .launch-faq h2 {
    margin: 0;
    font-size: clamp(0.88rem, 0.82rem + 0.2vw, 0.95rem);
    letter-spacing: -0.06em;
    line-height: 1.06;
    color: var(--launch-muted);
  }

  .launch-faq__intro {
    display: grid;
    gap: 0.9rem;
    max-width: 46rem;
  }

  .launch-faq__list {
    display: grid;
    gap: 0.2rem;
  }

  .launch-faq__item {
    padding: 0.05rem 0;
  }

  .launch-faq__item summary {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.75rem;
    align-items: center;
    padding: 0.7rem 0;
    list-style: none;
    font-size: 0.82rem;
    color: var(--launch-fg);
  }

  .launch-faq__item summary::-webkit-details-marker {
    display: none;
  }

  .launch-faq__item p {
    padding: 0 0 0.85rem 1.65rem;
    max-width: 48rem;
  }

  .launch-faq__icon {
    position: relative;
    width: 0.72rem;
    height: 0.72rem;
    color: var(--launch-muted);
    flex: 0 0 auto;
  }

  .launch-faq__icon span {
    position: absolute;
    inset: 50% auto auto 50%;
    width: 0.72rem;
    height: 1px;
    background: currentColor;
    transform: translate(-50%, -50%);
    transition: transform 180ms ease, opacity 180ms ease;
  }

  .launch-faq__icon span:last-child {
    transform: translate(-50%, -50%) rotate(90deg);
  }

  .launch-faq__item[open] .launch-faq__icon span:last-child {
    opacity: 0;
    transform: translate(-50%, -50%) rotate(90deg) scaleX(0.2);
  }
</style>
