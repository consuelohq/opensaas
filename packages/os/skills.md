# Consuelo OS skills

Skills are instruction resources generated into `skills/skills.json`. They guide use of the typed facade tools published from `tools/<domain>/`; they do not create same-named legacy actions.

The active Artifacts skill routes to `artifacts.*` tools and `scripts/artifacts.ts`. Data access, research, media, task, and site workflows use their current typed tools and app-native APIs.
