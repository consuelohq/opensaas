# workpad: DEV-1072 — [task] 13.2: TwiML App Auto-Provisioning
# created: 2026-03-04T00:09:24Z

## acceptance criteria

- [x] hosted: TwiML App created during sub-account provisioning (task 13.1) — already in provisionSubAccount()
- [x] hosted: TwiML App URL updated if API_BASE_URL changes — syncTwimlAppUrl in health check
- [x] BYOK: TwiML App auto-created when user saves credentials — ensureOrCreateTwimlApp in PUT handler
- [x] self-hosted: TwiML App auto-created or configurable via env var — auto-create in getDialerForWorkspace
- [x] token generation always uses the correct TwiML App SID per workspace — per-workspace Dialer with correct SID

## implementation notes

### files changed
- `packages/api/src/services/twilio-config.ts` — added ensureOrCreateTwimlApp, syncTwimlAppUrl helpers
- `packages/api/src/routes/twilio-settings.ts` — BYOK auto-creation in PUT handler, health check route
- `packages/api/src/shared/dialer.ts` — self-hosted auto-creation when TWILIO_TWIML_APP_SID unset

### key decisions
- ensureOrCreateTwimlApp searches by friendlyName "Consuelo Dialer" to avoid duplicates on restart
- BYOK auto-creation is best-effort — creds saved regardless, warning returned on failure
- self-hosted auto-create only runs if API_BASE_URL is set (needed for voiceUrl)
- health check at /v1/settings/twilio/health — syncs URL, re-creates if deleted

## improvements noticed

_none_
