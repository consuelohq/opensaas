# workpad: DEV-1080 — [task] 14.3: Skills Index Page (Full-Screen)
# created: 2026-03-04T03:55:51Z

## acceptance criteria

- [x] `/skills` shows all skills in a clean card grid
- [x] each skill card has name, description, status (active/draft badge)
- [x] clicking a card navigates to skill detail page (`/skills/:skillId`)
- [x] "Create Skill" button exists (IconPlus in PageHeader)
- [x] page uses twenty's standard layout components (PageContainer, PageHeader, PageBody)
- [ ] Add `Skills = '/skills'` to AppPath enum (`packages/twenty-shared/src/types/AppPath.ts`)
- [ ] Add route to useCreateAppRouter (`packages/twenty-front/src/modules/app/hooks/useCreateAppRouter.tsx`)
- [ ] Create page wrapper: `packages/twenty-front/src/pages/skills/SkillsIndexPage.tsx`
- [ ] Create `packages/twenty-front/src/modules/skills/` directory
- [ ] Create subdirectories: `components/`, `hooks/`, `states/`, `types/`
- [ ] `SkillsIndexPage.tsx` — main component (in modules/skills/components/)
- [ ] `SkillsGrid.tsx` — card grid layout
- [ ] `SkillCard.tsx` — individual skill card
- [ ] `SkillsSearchBar.tsx` — search + filter controls
- [ ] `SkillCategoryBadge.tsx` — category badge
- [ ] Use existing `useAgentSkills` hook from `@/agent/hooks/useAgentSkills`
- [ ] Handle loading state with skeleton cards
- [ ] Handle empty state
- [ ] Use Emotion styled components
- [ ] Use theme spacing, colors, fonts from `theme` object
- [ ] Grid: `display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));`
- [ ] Card: border, border-radius.sm, padding, hover effects
- [ ] Search by name/description
- [ ] Filter by category
- [ ] Sort by name or date
- [ ] Click card to navigate to skill detail/editor
- [ ] Status badge (active/draft/etc.)

## implementation notes

### research findings (2026-03-03)

previous agent (commit 702642e9b7) already implemented the basics:
- AppPath.Skills = '/skills' ✅ in AppPath.ts
- route registered in useCreateAppRouter.tsx ✅
- SkillsPage.tsx page wrapper ✅
- SkillsIndexPage.tsx with PageContainer, PageHeader, search, grid ✅
- SkillCard.tsx with name, description, category badge, click-to-navigate ✅

**gaps found:**
1. **missing status on cards** — AC #2 says "name, description, status" but card shows category, not status. SkillListItem from useAgentSkills doesn't have `enabled` field. need to add status display.
2. **missing PageBody** — AgentPage uses PageBody wrapper between PageHeader and content. SkillsIndexPage uses a raw styled div. should match the pattern.
3. **BROKEN IMPORT** — router imports `AgentSkillsPage` from `~/pages/agent/AgentSkillsPage` but that file doesn't exist. will fail typecheck. need to create it or remove the route.
4. **no i18n** — search placeholder and text strings not wrapped in lingui `t` macro

**approach:**
- add `enabled` field to SkillListItem type in useAgentSkills (matches AgentSkill type)
- add status badge to SkillCard showing active/draft
- wrap content in PageBody for consistency
- create AgentSkillsPage.tsx as redirect to /skills
- add lingui i18n to user-facing strings

## improvements noticed

_if you spot workflow issues, missing scripts, broken patterns — note them here. don't fix infrastructure during a feature task._

