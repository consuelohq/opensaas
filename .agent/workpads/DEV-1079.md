# workpad: DEV-1079 — [task] 14.2: Agent Chat Full-Screen Page Redesign
# created: 2026-03-04T03:49:21Z

## acceptance criteria

- [ ] agent page uses `PageContainer` + `PageHeader`
- [ ] no skills sidebar on the agent page
- [ ] clean, professional layout matching twenty's design language
- [ ] chat messages look mature (not childish)

## implementation notes

### research findings (2026-03-04)

**current state:** previous agent (DEV-1079 first pass) added PageContainer + PageHeader and removed skills sidebar. but:
- no PageBody wrapper — missing the noisy background + PagePanel border/radius treatment
- chat messages use transparent.medium/lighter with borders — doesn't match twenty's AIChatMessage
- AIChatMessage pattern: user=tertiary bg, sm radius, font-weight 500, secondary color, no border; assistant=transparent bg, no radius, font-weight 400, primary color, no border

**plan:**
1. AgentPage.tsx — add PageBody between PageHeader and content. StyledContent becomes flex-row inside PageBody>PagePanel
2. AgentChatPanel.tsx — restyle messages to match AIChatMessage exactly: remove borders, use tertiary/transparent bg, add code block styling, clean input area
3. AgentContextPanel.tsx — minor cleanup for PageBody integration

**key files studied:**
- `AIChatMessage.tsx` — the gold standard for message styling
- `PageBody.tsx` — wraps in PagePanel (border, radius, primary bg, noisy outer bg)
- `PagePanel.tsx` — flex column, overflow-y hidden, border radius md

## improvements noticed

_none yet_
