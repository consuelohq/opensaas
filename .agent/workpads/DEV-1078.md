# workpad: DEV-1078 — [task] 14.1: Create Workflows Navigation Section in Sidebar
# created: 2026-03-04T02:41:55Z

## acceptance criteria

- [ ] fixed nav shows ONLY Search + Settings
- [ ] Ask AI and Assistant items hidden
- [ ] Dialer appears first in workspace scrollable section, toggles sidebar
- [ ] Agent folder contains Chat and Skills as sub-items
- [ ] Chat navigates to /agent (existing full-screen agent page)
- [ ] Skills navigates to /agent/skills (new full-screen skills page)
- [ ] all "Mercury" → "Dialer" (sidebar title, settings breadcrumbs, nav labels)
- [ ] "Consuelo" label in agent chat updated
- [ ] existing workspace items unchanged below Agent folder
- [ ] keyboard shortcut ⌘D preserved for dialer
- [ ] 🚨**NOTES FROM KO IMPORTANT**🚨:
- [ ] All Mercury → Dialer renames complete
- [ ] All Consuelo → Agent/neutral text renames complete
- [ ] SettingsPath enum updated
- [ ] AppPath enum updated with AgentSkills
- [ ] Settings pages moved and renamed
- [ ] SettingsRoutes.tsx lazy imports updated
- [ ] useSettingsNavigationItems.tsx updated
- [ ] MainNavigationDrawerFixedItems.tsx has only Search + Settings
- [ ] MainNavigationDrawerScrollableItems.tsx has Dialer + Agent folder
- [ ] AgentSkillsPage.tsx created
- [ ] useCreateAppRouter.tsx has new route
- [ ] All remaining Mercury/Consuelo references updated
- [ ] No unused imports
- [ ] `bash scripts/code-review.sh` passes all 13 checks
- [ ] Navigation works: click Dialer toggles sidebar, Agent folder expands/collapses
- [ ] Settings pages load at new paths
- [ ] `/agent/skills` route loads AgentSkillsPage

## implementation notes

### research findings
- AppPath.ts: keep existing `Skills = '/skills'`, add `AgentSkills = '/agent/skills'`
- IconMessageCircle aliased as IconComment in twenty-ui/display — use IconComment for Chat
- IconBolt available in twenty-ui/display — use for Skills
- SettingsConsuelo.tsx is the Mercury landing page — rename to SettingsDialer.tsx
- 9 SettingsMercury*.tsx files + helper components stay in place (only SettingsMercury* files rename)
- NavigationDrawerSubItem = NavigationDrawerItem with indentationLevel=2
- NavigationDrawerItemsCollapsableContainer handles drawer collapse animation (isGroup for visual grouping)
- useCreateAppRouter has SkillsPage at AppPath.Skills — separate from our new AgentSkillsPage

### implementation order
1. Update enums (AppPath.ts, SettingsPath.ts) — these are dependencies
2. Rewrite MainNavigationDrawerFixedItems.tsx (Search + Settings only)
3. Rewrite MainNavigationDrawerScrollableItems.tsx (Dialer + Agent folder)
4. Move+rename settings pages (consuelo/ → dialer/, SettingsMercury* → SettingsDialer*)
5. Update SettingsRoutes.tsx (lazy imports + route mappings)
6. Update useSettingsNavigationItems.tsx (Mercury → Dialer)
7. Update text in DialerSidebar, AgentChatPanel, CallButton
8. Create AgentSkillsPage.tsx + register route
9. Grep for remaining Mercury/Consuelo references
10. Run code-review.sh + typecheck

## improvements noticed

_if you spot workflow issues, missing scripts, broken patterns — note them here. don't fix infrastructure during a feature task._

