# workpad: DEV-1075 — [task] 13.5: Complete Mercury Settings Stubs
# created: 2026-03-04T02:38:49Z

## acceptance criteria

- [ ] no mercury settings page shows "coming soon"
- [ ] each page has functional controls (even if backend is minimal)
- [ ] AI provider page lets user enter API key for coaching/transcription
- [ ] audio page lets user test mic/speaker

## implementation notes

### research findings (2026-03-04)

4 of 5 pages are already functional — only Profile is a true stub:
- Audio (AudioDeviceSettings.tsx): mic/speaker selection, level meters, test buttons ✅
- AI Provider (AIProviderSettings.tsx): API key entry, model selection, coaching/transcript toggles ✅
- Subscription (SubscriptionSettings.tsx): plan display, usage stats, Stripe portal ✅
- Notifications (PreferencesSettings.tsx): missed call, voicemail, coaching toggles, quiet hours ✅
- Profile (SettingsMercuryProfile.tsx): shows "coming soon" ❌

### plan

1. Add `ProfilePreferences` type to `preferences.ts` with fields: displayName, voicemailGreeting, timezone, workingHoursStart, workingHoursEnd, workingHoursEnabled
2. Add `profile` to `UserPreferences` type and `DEFAULT_PREFERENCES`
3. Rewrite `SettingsMercuryProfile.tsx` using same patterns as PreferencesSettings:
   - `useUserPreferences` hook for state + persistence
   - TextInput for display name
   - TextArea for voicemail greeting
   - Select for timezone (Intl.supportedValuesOf pattern)
   - Time inputs for working hours (quiet hours pattern)
   - H2Title + Card + Section for layout

### patterns followed
- PreferencesSettings.tsx: toggle cards, select cards, time inputs, useUserPreferences
- TextInput: from `@/ui/input/components/TextInput`
- TextArea: from `@/ui/input/components/TextArea`
- Select: from `~/modules/ui/input/components/Select`

## improvements noticed

_none yet_
