# workpad: DEV-1081 — [task] 14.4: Skill Detail Page with Editor
# created: 2026-03-04T04:49:49Z

## acceptance criteria

- [ ] `/skills/:id` shows full skill detail page
- [ ] breadcrumb navigation back to skills index
- [ ] tabs for Overview, Editor, History, Test
- [ ] editor tab has system prompt, output format, sandbox template editing
- [ ] page looks professional using twenty's layout system

## implementation notes

### approach
- rewrite SkillDetailPage.tsx stub (currently just PageContainer + PageHeader)
- route already exists: AppPath.SkillDetail registered in useCreateAppRouter.tsx
- SkillCard already navigates to `/skills/${id}`

### tab implementation
- use TabButton from twenty-ui/input with local useState for active tab
- 4 tabs: Overview, Editor, History, Test
- overview: read-only skill metadata display
- editor: reuse SkillEditorForm from agent/components/skill-editor/
- history + test: stubs with placeholder text

### layout
- PageContainer + PageHeader + PageBody (same as SkillsIndexPage, AgentPage)
- breadcrumb: clickable "Skills ›" link in header area using navigate(-1) or navigate to /skills

### data
- useAgentSkills() hook fetches skills list, filter by skillId from useParams
- skill fields available: id, name, description, icon, category, enabled
- API returns 501 until DEV-948 storage layer — graceful handling needed

### files to modify
- packages/twenty-front/src/pages/skills/SkillDetailPage.tsx — full rewrite

## improvements noticed

_none yet_
