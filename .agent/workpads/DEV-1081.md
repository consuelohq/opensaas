# workpad: DEV-1081 — [task] 14.4: Skill Detail Page with Editor
# created: 2026-03-04T09:36:40Z

## acceptance criteria

- [x] `/skills/:id` shows full skill detail page
- [x] breadcrumb navigation back to skills index
- [x] tabs for Overview, Editor, History, Test
- [x] editor tab has system prompt, output format, sandbox template editing
- [x] page looks professional using twenty's layout system

## verification results

### code-review.sh — ALL 14 CHECKS PASSED
all 13 automated checks + spec compliance confirmed.

### typecheck
twenty-front typecheck fails but NO errors in skill detail page files. all failures are pre-existing in other modules (dialer, files, settings, GHL).

### acceptance criteria detail

1. **route**: `AppPath.SkillDetail = '/skills/:skillId'` in twenty-shared, route registered in useCreateAppRouter, SkillCard navigates to it
2. **breadcrumb**: styled breadcrumb in PageHeader — "Skills > Skill Name" with click handler navigating to AppPath.Skills
3. **tabs**: 4 TabButton components (Overview, Editor, History, Test) with icons, managed by useState<SkillDetailTab>
4. **editor tab**: renders SkillEditorForm which includes SystemPromptEditor, OutputFormatSelect, SandboxTemplateEditor, SkillCategorySelect
5. **layout**: PageContainer + PageHeader + PageBody with emotion styled components, loading state, unknown skill handling

## implementation notes

- SkillEditorForm accepts optional `skillId` prop but we don't pass it yet — initial values will be wired when DEV-948 storage layer is complete
- useAgentSkills returns `SkillListItem` type (id, name, description, icon, category, enabled, folderId) — page uses category and enabled for overview tab
- History and Test tabs are placeholders with descriptive messages about when they'll be available
- all text localized with Lingui `t` template tag

## improvements noticed

_none_
