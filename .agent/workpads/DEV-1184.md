# workpad: DEV-1184 — Move Dialer/Agent into Workspace section + turn on favorites
# created: 2026-03-04T09:39:33Z

## acceptance criteria

- [ ] Dialer appears as FIRST item in Workspace section
- [ ] Agent appears as LAST item in Workspace section (after all workspace items)
- [ ] Dialer click still toggles the dialer sidebar panel
- [ ] Agent still has collapsible Chat/Skills subitems
- [ ] Search and settings remain in fixed top section (unchanged)
- [ ] Workspace items render normally between Dialer and Agent
- [ ] Drag-and-drop still works for regular workspace items
- [ ] No TypeScript errors
- [ ] code-review.sh passes all 13 checks
- [ ] Favorites/navigation menu items always on (feature flag bypassed)

## implementation plan

### approach: headerContent/footerContent props

NOT using pseudo-workspace items (too complex). Instead:
1. Add `headerContent` and `footerContent` ReactNode props to NavigationDrawerSectionForWorkspaceItems
2. Render them inside the section, before/after the Droppable, within isNavigationSectionOpen
3. Pass Dialer JSX as headerContent, Agent JSX as footerContent from WorkspaceNavigationMenuItems
4. Remove Dialer/Agent from MainNavigationDrawerScrollableItems
5. Bypass IS_NAVIGATION_MENU_ITEM_ENABLED in both dispatchers

### files to modify (5 files)

1. `packages/twenty-front/src/modules/object-metadata/components/NavigationDrawerSectionForWorkspaceItems.tsx`
   - Add headerContent/footerContent props
   - Render before/after Droppable inside isNavigationSectionOpen

2. `packages/twenty-front/src/modules/navigation-menu-item/components/WorkspaceNavigationMenuItems.tsx`
   - Import dialer state + agent icons/routes
   - Create Dialer and Agent JSX
   - Pass as headerContent/footerContent

3. `packages/twenty-front/src/modules/navigation/components/MainNavigationDrawerScrollableItems.tsx`
   - Remove Dialer NavigationDrawerSection (lines 50-57)
   - Remove Agent NavigationDrawerSection (lines 59-77)
   - Remove all unused imports

4. `packages/twenty-front/src/modules/navigation-menu-item/components/WorkspaceNavigationMenuItemsDispatcher.tsx`
   - Always render WorkspaceNavigationMenuItems (bypass feature flag)

5. `packages/twenty-front/src/modules/navigation-menu-item/components/CurrentWorkspaceMemberNavigationMenuItemFoldersDispatcher.tsx`
   - Always render CurrentWorkspaceMemberNavigationMenuItemFolders (bypass feature flag)

## implementation notes

- Dialer state: uses existing `dialerSidebarOpenState` recoil atom
- Agent state: local useState for folder open/close (same as current)
- headerContent renders BEFORE the Droppable (not draggable)
- footerContent renders AFTER the Droppable (not draggable)
- Both only visible when section is open (inside isNavigationSectionOpen check)
- Feature flag IS_NAVIGATION_MENU_ITEM_ENABLED checked in ~20 places — only changing the 2 dispatchers for now

## improvements noticed

_if you spot workflow issues, missing scripts, broken patterns — note them here._
